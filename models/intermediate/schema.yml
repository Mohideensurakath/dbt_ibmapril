version: 2

models:
  - name: int_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
          
  - name: int_part_supps_statistics
    tests:
      - currency_conversion

  - name: int_lineitem
    tests:
      - currency_conversion

  - name: int_orders
    tests:
      - currency_conversion